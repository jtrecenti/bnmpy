[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: marks tests as integration tests (requires real API access)
    browser: marks tests that require browser interaction
addopts = 
    -v
    --tb=short
    --strict-markers

